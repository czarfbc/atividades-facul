import javax.swing.JOptionPane;

public class App {
    public static void main(String[] args) throws Exception {
        // 1-a

        // 1-b
        // int sal;
        // double desc;
        // String ssal;
        // ssal = JOptionPane.showInputDialog(null, "digite um salario");
        // sal = Integer.parseInt(ssal);
        // if (sal >= 2300 && sal <= 5000) {
        // desc = sal * 0.925;
        // JOptionPane.showMessageDialog(null, desc);
        // } else if (sal > 5000 && sal <= 7000) {
        // desc = sal * 0.91;
        // JOptionPane.showMessageDialog(null, desc);
        // } else if (sal > 7000 && sal <= 9000) {
        // desc = sal * 0.89;
        // JOptionPane.showMessageDialog(null, desc);
        // } else if (sal > 9000) {
        // desc = sal * 0.86;
        // JOptionPane.showMessageDialog(null, desc);
        // } else {
        // JOptionPane.showMessageDialog(null, "sem desconto");
        // }
    }
}
